# Nexus Discord Event Bot - Production Dependencies
# Pinning with semantic upper bounds to prevent breaking upstream API changes

discord.py>=2.6.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0
python-dateutil>=2.8.2,<3.0.0
asyncpg>=0.29.0,<1.0.0
